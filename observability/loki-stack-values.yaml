grafana:
  enabled: false

prometheus:
  enabled: false

loki:
  enabled: true
  isDefault: false
  persistence:
    enabled: false

promtail:
  enabled: true
  config:
    clients:
      - url: http://loki:3100/loki/api/v1/push
